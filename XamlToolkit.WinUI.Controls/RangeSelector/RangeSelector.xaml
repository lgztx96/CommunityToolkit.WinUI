<!--  Licensed to the .NET Foundation under one or more agreements. The .NET Foundation licenses this file to you under the MIT license. See the LICENSE file in the project root for more information.  -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:XamlToolkit.WinUI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:not_win="http://uno.ui/not_win"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    mc:Ignorable="not_win">

    <Style x:Key="RangeSelectorThumbStyle" TargetType="Thumb">
        <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{ThemeResource SliderThumbBorderBrush}" />
        <!--  Uno specific (https://github.com/unoplatform/uno/issues/6457): Use solid border brush  -->
        <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
        <Setter Property="Background" Value="{ThemeResource SliderThumbBackground}" />
        <Setter Property="Height" Value="{ThemeResource SliderHorizontalThumbHeight}" />
        <Setter Property="Width" Value="{ThemeResource SliderHorizontalThumbWidth}" />
        <Setter Property="CornerRadius" Value="{ThemeResource SliderThumbCornerRadius}" />
        <Setter Property="FocusVisualMargin" Value="-7" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border
                        Margin="-2"
                        Background="{ThemeResource SliderOuterThumbBackground}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                        <Ellipse
                            x:Name="SliderInnerThumb"
                            Width="{ThemeResource SliderInnerThumbWidth}"
                            Height="{ThemeResource SliderInnerThumbHeight}"
                            Fill="{TemplateBinding Background}"
                            RenderTransformOrigin="0.5, 0.5">
                            <Ellipse.RenderTransform>
                                <CompositeTransform />
                            </Ellipse.RenderTransform>
                        </Ellipse>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                            <!--  0.86 is relative scale from 14px to 12px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlFastAnimationDuration}"
                                                Value="0.86" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                            <!--  0.86 is relative scale from 14px to 12px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlFastAnimationDuration}"
                                                Value="0.86" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlNormalAnimationDuration}"
                                                Value="1.167" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlNormalAnimationDuration}"
                                                Value="1.167" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="Fill">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SliderThumbBackgroundPointerOver}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                            <!--  0.71 is relative scale from 14px to 10px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlNormalAnimationDuration}"
                                                Value="0.71" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                            <!--  0.71 is relative scale from 14px to 10px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlNormalAnimationDuration}"
                                                Value="0.71" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="Fill">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SliderThumbBackgroundPressed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleX)">
                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlFastAnimationDuration}"
                                                Value="1.167" />
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="SliderInnerThumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.ScaleY)">
                                            <!--  1.167 is relative scale from 12px to 14px  -->
                                            <SplineDoubleKeyFrame
                                                KeySpline="{StaticResource ControlFastOutSlowInKeySpline}"
                                                KeyTime="{StaticResource ControlFastAnimationDuration}"
                                                Value="1.167" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultRangeSelectorStyle}" TargetType="controls:RangeSelector" />

    <Style x:Key="DefaultRangeSelectorStyle" TargetType="controls:RangeSelector">
        <Setter Property="Background" Value="{ThemeResource SliderTrackFill}" />
        <Setter Property="BorderThickness" Value="{ThemeResource SliderBorderThemeThickness}" />
        <Setter Property="Foreground" Value="{ThemeResource SliderTrackValueFill}" />
        <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
        <Setter Property="CornerRadius" Value="{ThemeResource SliderTrackCornerRadius}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:RangeSelector">
                    <Grid x:Name="ControlGrid" Height="{ThemeResource SliderHorizontalHeight}">
                        <Border
                            x:Name="BackgroundElement"
                            Height="4"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                        <Canvas x:Name="ContainerCanvas" Background="{ThemeResource SliderContainerBackground}">
                            <Rectangle
                                x:Name="ActiveRectangle"
                                Height="4"
                                HorizontalAlignment="Stretch"
                                Fill="{TemplateBinding Foreground}" />
                            <Thumb
                                x:Name="MinThumb"
                                Margin="0,7,0,0"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="Min thumb"
                                IsTabStop="True"
                                Style="{StaticResource RangeSelectorThumbStyle}"
                                TabIndex="0" />
                            <Thumb
                                x:Name="MaxThumb"
                                Margin="0,7,0,0"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="Max thumb"
                                IsTabStop="True"
                                Style="{StaticResource RangeSelectorThumbStyle}"
                                TabIndex="1" />
                        </Canvas>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal">
                                    <VisualState.Setters>
                                        <Setter Target="MinThumb.Background" Value="{ThemeResource SliderThumbBackground}" />
                                        <Setter Target="MaxThumb.Background" Value="{ThemeResource SliderThumbBackground}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="ActiveRectangle.Fill" Value="{ThemeResource SliderTrackValueFillPointerOver}" />
                                        <Setter Target="MinThumb.Background" Value="{ThemeResource SliderThumbBackgroundPointerOver}" />
                                        <Setter Target="MaxThumb.Background" Value="{ThemeResource SliderThumbBackgroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="MinPressed">
                                    <VisualState.Setters>
                                        <Setter Target="ActiveRectangle.Fill" Value="{ThemeResource SliderTrackValueFillPressed}" />
                                        <Setter Target="MinThumb.Background" Value="{ThemeResource SliderThumbBackgroundPressed}" />
                                        <Setter Target="MaxThumb.Background" Value="{ThemeResource SliderThumbBackgroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="MaxPressed">
                                    <VisualState.Setters>
                                        <Setter Target="ActiveRectangle.Fill" Value="{ThemeResource SliderTrackValueFillPressed}" />
                                        <Setter Target="MinThumb.Background" Value="{ThemeResource SliderThumbBackgroundPressed}" />
                                        <Setter Target="MaxThumb.Background" Value="{ThemeResource SliderThumbBackgroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="BackgroundElement.Background" Value="{ThemeResource SliderTrackFillDisabled}" />
                                        <Setter Target="ActiveRectangle.Fill" Value="{ThemeResource SliderTrackValueFillDisabled}" />
                                        <Setter Target="MinThumb.Background" Value="{ThemeResource SliderThumbBackgroundDisabled}" />
                                        <Setter Target="MaxThumb.Background" Value="{ThemeResource SliderThumbBackgroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="OrientationStates">
                                <VisualState x:Name="Horizontal" />
                                <VisualState x:Name="Vertical">
                                    <VisualState.Setters>
                                        <Setter Target="ControlGrid.Width" Value="{ThemeResource SliderVerticalWidth}" />
                                        <Setter Target="ControlGrid.Height" Value="Auto" />
                                        <Setter Target="ActiveRectangle.Width" Value="4" />
                                        <Setter Target="ActiveRectangle.HorizontalAlignment" Value="Center" />
                                        <Setter Target="ActiveRectangle.VerticalAlignment" Value="Stretch" />
                                        <Setter Target="MinThumb.Margin" Value="7,0,0,0" />
                                        <Setter Target="MaxThumb.Margin" Value="7,0,0,0" />
                                        <Setter Target="BackgroundElement.Height" Value="Auto" />
                                        <Setter Target="BackgroundElement.Width" Value="4" />
                                        <Setter Target="BackgroundElement.HorizontalAlignment" Value="Center" />
                                        <Setter Target="BackgroundElement.VerticalAlignment" Value="Stretch" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
